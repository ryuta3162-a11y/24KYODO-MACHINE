# 明日の別PC用メモ（2026-09-13 夜締め）

経堂リニューアル配置アプリの引き継ぎ。別PCで続きをやるときは、まずこのファイルとリンクを開く。

---

## すぐ開くリンク

| 用途 | URL |
|---|---|
| **配置アプリ（本番）** | https://24kyodo-machine.vercel.app/floorplan-app/?room=kyodo-2f |
| 短縮パス | https://24kyodo-machine.vercel.app/plan/?room=kyodo-2f |
| GitHub | https://github.com/ryuta3162-a11y/24KYODO-MACHINE |
| スプレッドシート「経堂アイデアプール」 | https://docs.google.com/spreadsheets/d/1YR4UNjOHT-AManewnSOEPxuR01kBVwXfgoCAjDsPeOw/edit |
| └ 既存マシン | 同シート「既存マシン」タブ |
| └ 新マシン（計画フォーマット） | 同シート「新マシン」タブ |
| Vercel プロジェクト | https://vercel.com/ryuta0808s-projects/24kyodo-machine |

### 別PCセットアップ（最短）

```bash
git clone https://github.com/ryuta3162-a11y/24KYODO-MACHINE.git
cd 24KYODO-MACHINE
npm install
# 本番は既に Vercel。ローカル確認は静的配信 or vercel dev
npx vercel --prod   # 変更を出すとき（要ログイン）
```

Google アカウントは **r-kusaka@okamoto-group.co.jp**（会社）。個人 Gmail ではない。

---

## 今日までできたこと（要点）

### アプリ
- 施工図上にマシンを実寸配置（**1px = 1cm**、図面 **3388×2058**）
- 共有ルーム `kyodo-2f`（保存は Vercel Blob `/api/room`）
- フィルタ: **既存 / 新規** ＋ ジャンル（有酸素 / HYROX / スタック / プレート / FW）
- ダブルクリックで **本体切取**（周囲80cmクリアランス解除）／Ctrl+Z
- 保存後は「保存されました」トースト。保存直後の空読み込みで消える不具合は修正済み

### 表示ルール（重要）
- **写真 = 本体サイズのみ**（中央）
- **周囲 80cm = 4マス分は線枠**（写真を伸ばさない／有酸素と同じ考え方）
- 寸法矢印は **本体の m 表記**（例: レッグプレス 1.06×1.72m）
- 区画込みサイズは `本体 + 80×2`（例: 2.66×3.32m）だが、写真はその中に収めない

### 画像
- **既存レジスタンス** `resistance_*.jpg` → place/preview 再生成済み
- **既存 FW** `freeweight_*.jpg` → 同様に再生成済み
- **新規** 約53台: 商品ページから取得。Evolgear は `/img/product/{sku}.webp` を優先
- まだ弱いもの: C50 / Standing Abductor（ラベルカード）、一部 Rogue・Tire など

### スプレッドシート
- 新マシンは計画フォーマット列あり（層・ゾーン候補・図面幅_cm など）
- **層（コア/推奨…）は今は使わない**。配置しながら明日相談

---

## 明日やること（優先順）

1. **別PCでアプリを開き、配置を続き**  
   https://24kyodo-machine.vercel.app/floorplan-app/?room=kyodo-2f  
   → 「最新」でクラウド配置を読む。保存前に名前欄を入れると履歴が分かりやすい。

2. **既存＋新規を並べて配置案を作る**（層分けは後回し）  
   - 既存: スタック / FW / 有酸素  
   - 新規: プレート・HYROX・追加スタックなど

3. **画像の残り直し**（必要なら）  
   - 有酸素 `cardio_*` も写真化するか決める（今は上面簡略図のまま）  
   - C50 / Standing Abductor などラベル落ちの差し替え

4. **予算・採用の線引き**（明日の議論用）  
   - スプレッドシートの「層」を使うかは相談後  
   - Assist Dip の重複候補（HS vs Matrix）はどちらか

5. **任意**  
   - Google Drive への画像ギャラリー（会社アカウントの clasp 認可が必要。Workspace で未検証アプリがブロックされやすい）  
   - PNG 書き出しで提案資料化

---

## コードの場所

| パス | 内容 |
|---|---|
| `floorplan-app/` | 配置 UI（`app.js` / `style.css` / `index.html`） |
| `api/machines.js` | 既存＋新規マシン API（Sheets CSV） |
| `api/room.js` | ルーム保存（Blob） |
| `scripts/rebuild_existing_from_photos.mjs` | 既存写真→place 再生成（`freeweight` / `resistance` / `cardio`） |
| `scripts/fetch_new_machine_images.mjs` | 新規の商品画像取得 |
| ルート `resistance_*.jpg` / `freeweight_*.jpg` / `cardio_*.jpg` | 元写真 |

スケール再確認: **1px = 1cm**。クリアランス既定 **80cm**（20cm格子で **4マス**）。

---

## 注意

- 保存はクラウド共有。誤って「クリア」→保存すると全員空になるので注意
- 強リロード（Ctrl+F5）しないと古い画像キャッシュが残ることがある（`ART_VER` 付き）
- clasp で Sheets 直接書き込みは会社 Workspace の制限で詰まりやすい。スプシはブラウザ編集か Apps Script をシート内で

---

更新: 2026-09-13（今日終了時点）
