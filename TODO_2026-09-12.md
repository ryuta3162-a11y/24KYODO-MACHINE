# 2026-09-12 やってほしいこと（メモ）

このファイルは、別PC（自宅ゲーミングPC）でリポジトリをクローンしたあとに続きをやるための引き継ぎメモです。

関連リポジトリ: **24KYODO-MACHINE**  
ローカル例: `Documents/GitHub/24KYODO-MACHINE`

---

## 背景（今日やったこと）

- 施工図（横3000cm × 縦1900cm、1マス=20cm、150×95マス）の上に、マシンを実寸で置く作業をしている。
- LP画像（`cardio_*.jpg` / `resistance_*.jpg` / `freeweight_*.jpg`）から、上面＋外枠（四方0.80m区画）＋寸法表記の統一モジュールを生成した。
- 生成物のZIP: デスクトップの `施工図マシン_Canva用.zip`（中の `place/*.png` がCanva貼付用、1px=1cm）。
- Canvaでは画像が自動リサイズされ、1枚ずつ「配置」でpx指定する必要があり **効率が悪い** と判明。

関連コード・データ:
- `build_floorplan_assets.py` … モジュール一括生成
- `floorplan/machines_catalog.json` … マシン名・寸法（暫定含む）
- `floorplan/machines/` … place / preview
- `floorplan/CANVA.md` … Canva運用メモ

---

## これから作りたいもの（本命）

### Canva風のマシン配置Webアプリ（Vercel想定）

先方には「Canvaみたいに操作できる」のが理想、と言われている。  
一方で実寸の正確さはWebの方が強い。**両立するサイトを作る。**

#### 必須要件
1. 背景に施工図（公式の格子図面 / 3000×1900cm基準）を表示
2. マシン一覧から選んで、図面上にドラッグ＆ドロップで配置
3. **サイズは最初から実寸固定**（Canvaのように勝手に拡大されない）
   - スケール: 1マス=20cm、四方クリアランス0.80m込みのモジュールサイズ
4. 移動・回転は可（リサイズは基本不可、またはロック）
5. 配置結果を **PNG書き出し**（必要ならその1枚をCanvaに載せる）
6. 見た目のマシンモジュールは、今日作った統一デザイン（外枠・寸法・名前）を踏襲

#### あると良い
- 配置の保存（JSON）／読み込み
- マシンの表示ON/OFF、数量分の複製
- スマホでも一応触れる（先方共有を見据える）
- 寸法は `machines_catalog.json`（またはスプシ連携）を正とする

#### 非目標（今はやらなくてよい）
- Canvaプラグイン化
- 管理画面の本格化
- 立体表示

---

## 技術方針（仮）

- ホスト: **Vercel**
- フロント: 既存のマシンLPサイト（このリポの `index.html`）とは別に、`floorplan-app/` など新規でも可
- ローカルにNodeが無い環境でも、Vercel側でビルドできればOK
- 元画像（LP jpg）はこのリポジトリに既にあるのでそれを使う

---

## 自宅PCで再開するとき

1. このリポジトリをクローン（または `git pull`）
2. **このファイル（本メモ）を読む**
3. エージェントに「`TODO_2026-09-12.md` に従って配置Webを作って」と依頼
4. できれば公式施工図画像も `floorplan/source/` に置く（ファイル名例: `floor_official_3000x1900.jpg`）

---

## 優先順位

1. 配置Webの最小版（図面＋マシンDnD＋実寸固定＋PNG出力）
2. VercelデプロイしてURL共有
3. 先方向けに操作を簡単化（保存・書き出しボタンを目立たせる）
4. スプシ寸法の確定値でカタログ更新

---

## 補足

- 寸法の一部は暫定値。スプシ確定後に `machines_catalog.json` を更新してモジュール再生成。
- Canva手作業ルートは「緊急避難」扱い。本線は配置Web。
